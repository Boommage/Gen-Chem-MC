import PageHeader from '../components/PageHeader'

export default function Progress() {
  return <PageHeader title="Progress" subtitle="Notice patterns over time without turning every study session into a score." />
}
